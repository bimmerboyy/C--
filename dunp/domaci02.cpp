#include <iostream>
#include <string.h>

using namespace std;

#define MAX 20

class Student {
    public:
        string firstName, lastName, smer;
        int ocene[MAX];
        Student(string fn, string ln, string s){
            firstName = fn;
            ln = lastName;
            smer = s;
        }
        float prosek(){
            int s = 0;
            int l = sizeof(ocene) / sizeof(int);
            for()
        }
}