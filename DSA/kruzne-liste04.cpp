/*#include<stdio.h>
#include<stdlib.h>

typedef struct Node{
    int info;
    struct Node *next;
}NODE;

NODE* front,*rear,temp;

NODE* pom(NODE* p,NODE *eksponent){
    struct Node* l1;
    temp = *l1;
    struct Node *l2 = (struct Node*)malloc(sizeof(struct Node));
    struct Node *pom = (struct Node*)malloc(sizeof(struct Node));
    while(temp != rear){
        temp->info = pom;
    }

}
*/